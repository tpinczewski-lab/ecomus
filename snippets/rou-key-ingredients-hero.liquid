{% comment %}
  Wspólny blok kart produktu (używany w 32 miejscach w szablonach product.*).
  Edytuj TUTAJ — zmiana obejmie wszystkie karty produktów, które go renderują.
  Wywołanie: {% render 'rou-key-ingredients-hero', product: product, section_id: section.id %}
{% endcomment %}
<style>
  .ingredients-wrapper {
    padding-top: 50px; /* Odstęp od góry na desktop */
  }

  @media (max-width: 480px) {
    .ingredients-wrapper {
      padding-top: 20px; /* Odstęp od góry na mobile */
    }
  }
</style>

<div class="ingredients-wrapper" style="font-size: clamp(15px, 4vw, 20px); line-height: 1.3;">
  {{ product.metafields.custom.kluczowe_sk_adniki }}
</div>
